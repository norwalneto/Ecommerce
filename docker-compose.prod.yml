version: "3.8"
services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQLPASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQLUSER}
      MYSQL_PASSWORD: ${MYSQLPASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: springboot-app
    depends_on:
      - mysql
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      DB_HOST: mysql.railway.internal
      DB_PORT: 3306
      DB_DATABASE: railway
      DB_USER: root
      DB_PASSWORD: ffAABKTqwguSLbLtsSmdMbVvKxVuusWS
      DB_URL: mysql://root:ffAABKTqwguSLbLtsSmdMbVvKxVuusWS@switchback.proxy.rlwy.net:46697/railway
volumes:
  mysql_data:
